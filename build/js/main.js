"use strict";
// interface mathFunction {
//   (a: number, b: number): number;
// }
let multiply = function (c, d) {
    return c * d;
};
console.log(multiply(5, 5));
// Optional Parameters
const addAll = (a, b, c) => {
    return a + b + c;
};
